# catalog-typescript CDK fixture

Construct catalog layout: `typescript/aws/<construct>/` with `.stackgen/stackgen.yaml`.
`aws_s3_versioned_bucket/` is intentionally empty for Template I scaffold tests (T3).

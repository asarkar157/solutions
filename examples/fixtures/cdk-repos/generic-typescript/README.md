# generic-typescript CDK fixture

Minimal AWS CDK v2 TypeScript app used by `aios-agent-cdk-bot` CI (`stage_runner_logic_test.sh`) and manual workflow tests (scenario T1/T2/T5).

Run locally: `npm ci && npm test && npx cdk synth --no-staging`

# GitOps incident scope

CCE + GitOps SRE demo — scoped Argo CD rollback recommendations.

```bash
make demo SCENARIO=gitops-incident-scope
```

Requires pre-provisioned Guild secrets for GitLab, Argo CD, SonarQube, AWS, Slack.

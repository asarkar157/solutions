# CloudFormation knowledge base

Optional secure-synthesis references for [`aios-agent-cfn-author`](../../modules/aios-agent-cfn-author/) (`knowledge_base_path`, default `cloudformation/knowledge-base/`).

Add org-specific hardening notes, tagging standards, and approved resource patterns as markdown files. The hardened-synthesis runbook reads this path during template generation.

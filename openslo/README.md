# OpenSLO catalog (local dev)

Sample SLO definitions for the **SLO Health** agent (`slo-definition-bootstrap`, `slo-health-review`).

Layout follows [Sloth](https://sloth.dev/) / OpenSLO conventions — see `modules/aios-agent-slo-health/docs/OPENSLO_REPO_LAYOUT.md`.

Terraform default for guild local dev:

- Repo: `appcd-dev/solutions`
- Path prefix: `openslo/`
- Branch: `main`

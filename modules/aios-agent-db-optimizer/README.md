# DB Optimizer Agent

This module provisions an agent to act as a Database Reliability Engineer (DBRE).
It analyzes slow query logs, runs `EXPLAIN ANALYZE`, and recommends schema modifications and indexing strategies to maintain database performance.

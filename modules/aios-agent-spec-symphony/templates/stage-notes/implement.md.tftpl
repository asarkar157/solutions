Implement per tasks.md or openspec change record on runner clone. Spawn `implement-spec-feature` by agent_name only.

Close: copy `implement_summary=` and `implement_edit_verified=true` from subagent stdout. If markers missing → `stage_summary:implement=blocked: missing implement markers`.

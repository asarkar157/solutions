Spawn `materialize-spec-runner` — ONE `${shell_tool_prefix}_execute_series` with spawn-context Materialize command.

read_notes repo_clone_path spec_markdown engineering_subgoals linear_issue_id. note spec_tasks_path from stdout.

FORBIDDEN: implement before materialize completes.

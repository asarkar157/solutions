Spawn `post-linear-implement-comment` by agent_name only. read_notes `linear_issue_id`, `pr_url`, `ci_status`, `engineering_subgoals`.

Use `${linear_tool_save_comment}` with PR link, CI status, and subgoal checklist.

note `linear_implement_comment_posted=true`.

FORBIDDEN: gh issue comment when linear_issue_id is set.

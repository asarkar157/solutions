Spawn `fetch-spec-context-reader` by agent_name only. read_notes `linear_issue_id`.

Use `${linear_tool_list_comments}`; locate comment with `<!-- spec-symphony-spec-v1 -->`. note `spec_markdown`, `engineering_subgoals`, `spec_comment_found=true|false`.

If not found: note `fetch_spec_blocker=missing_spec_comment`.

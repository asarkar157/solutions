Author spec/plan/tasks from ticket + constitution when missing. Spawn `author-spec-runner` or `author-spec-cursor-runner` by agent_name only.

Close: copy `author_spec_status=`, `spec_tasks_path=`, `spec_artifact_path=` from subagent stdout. If `author_spec_blocker=` → `stage_summary:author-spec=blocked`.

Framework: ${sdd_framework}. Change type: ${change_type}.

package policy

# Sandbox network allowlisting previously used `input.context.sandbox` and
# `input.context.network_activity`, which are not part of the standard policy
# evaluation input.

default approval_required := false

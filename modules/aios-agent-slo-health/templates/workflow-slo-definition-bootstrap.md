Discover SLO definitions from Grafana dashboards and alert rules, validate PromQL, draft OpenSLO YAML, open GitHub PR for human review.

**Git repo / path** are fixed by Terraform stage notes — do not pass a different `Repo:` in chat.

Requires Ubuntu CLI or remote runner for the PR stage only. Set workflow input `confirm_pr=true` before PR stage.

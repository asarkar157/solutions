Notify that the OpenSLO bootstrap PR was opened.

Post to Slack (${slack_channel_hint}) with PR link, proposal count, and one-line summary per SLO.

Optional: include `slo_proposals_validated` JSON snippet for webhook consumers.

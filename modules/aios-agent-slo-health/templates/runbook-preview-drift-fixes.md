Preview drift reconcile changes before PR.

Side-by-side summary: drift type, action, git path, plain-English suggestion.

Require **`confirm_pr=true`** before open-drift-pr stage.

List SUGGEST_GRAFANA_CHANGE items as read-only recommendations (not in PR).

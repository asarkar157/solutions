# AIOS Integration — MySQL

Provisions a MySQL `sg_secret` (`Database`/`mysql`) and `sg_guild_integration` using the Guild MCP Toolbox sidecar.

Metadata keys match vault `KeyDefinitions`: `host`, `port`, `database`, `user`, `password`.

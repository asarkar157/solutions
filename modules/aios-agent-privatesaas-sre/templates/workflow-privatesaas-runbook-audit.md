Read-only PrivateSaaS runbook audit: inventory runbooks across module SOPs, FireHydrant, internal tooling, and external catalog entries, then report coverage gaps.

- **Environment**: ${private_saas_environment_label}
